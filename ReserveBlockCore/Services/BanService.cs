namespace ReserveBlockCore.Services
{
    public class BanService
    {
        public static void BanPeer()
        {
            //ban peer based on IP
        }
        public static void BanAddress()
        {
            //Ban peer based on Address
            //This will also prevent this address from sending out if determined by adj's.
        }
    }
}
